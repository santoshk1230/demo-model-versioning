# Production requirements for AWS Lambda
# These dependencies are Lambda-safe (total size < 250MB zipped)
# Excludes pandas, numpy, and related heavy packages

pydantic>=2.0
boto3
email-validator
rapidfuzz
